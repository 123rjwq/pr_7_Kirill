// Интерфейс Drink объявляет функцию drinkSale(), которая представляет собой метод для продажи напитков в пиццерии.
// При реализации этого интерфейса классом необходимо будет переопределить функцию drinkSale() с нужной логикой,
interface Drink {
    fun drinkSale()
}